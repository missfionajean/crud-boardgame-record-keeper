<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1.0" />
		<link rel="stylesheet" href="/css/style.css" />
		<title>Document</title>
	</head>
	<body>
		<section class="page-head">
			<h1>The Den of Heroes</h1>
			<p>Click hero name for to view/edit profile:</p>
		</section>
		<section id="hero-roster">
			<ul>
				<% players.forEach((player) => { %>
					<li>
						<img src=<%= player.playerAvatar %> alt="Player Avatar" class="inline-avatar">
						<a href="/heroes/<%= player._id %>"><%= player.playerName %> the <%= player.playerTitle %></a>
					</li>
				<% }) %>
			</ul>
		</section>
		<div class="button-box">
			<form action="/heroes/new">
				<button type="submit">Create New Profile</button>
			</form>
			<form action="/">
				<button type="submit">Return To Lobby</button>
			</form>
		</div>
	</body>
</html>
